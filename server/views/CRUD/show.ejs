<!DOCTYPE html>
<html>
    <head>
        <!-- Bootstrap 5.3.2 CDN -->
        <!-- Latest compiled and minified CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <!-- Latest compiled JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        <title><%=assignToShow.name%> - <%=assignToShow.class%></title> <!-- Display assignment name and class in title -->
        <meta charset="UTF-8">
    </head>
    <body>
        <div>
            <h3><%=assignToShow.name%></h3>
            <p><%=assignToShow.class%></p>
            <p>Due on: <%=assignToShow.due%></p>
            <p><%=assignToShow.notes%></p>   
            <div class="mb-1"> <!--Note input-->
            <form class="form container d-flex flex-column" method="post">
                <label for="assignNotes" class="form-label">Notes:</label><br>
                <textarea name="notes" title="Notes" id="assignNotes" class="textarea form-control" required></textarea>
                <button class="btn btn-outline-success" type="submit">Submit</button>
            </form>
            </div> 
            <p><%=assignToShow.mark%></p>
            <p><%=assignToShow.weight%></p>
        </div>
        <div>
            <a href="/list/edit/<%= assignToShow._id %>" 
                class="btn btn-warning btn-sm"> Edit
            </a>  
            <a href="/list/delete/<%= assignToShow._id %>" 
                class="btn btn-danger btn-sm"> Delete
            </a>
        </div>
    </body>
</html>